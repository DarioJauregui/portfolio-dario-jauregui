---
interface Props {
  title: string;
  intro?: string;
}
const { title, intro } = Astro.props;
---

<header class="section-head">
  <h2>{title}</h2>
  {intro && <p class="section-intro">{intro}</p>}
</header>
